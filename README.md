# Simple Signup

A super-simple signup email collector app

Place an alternative 'index.html' file into the working directory if you want anything custom.

Outputs to signups.csv by default.

See code for CLI Arg options.